# AutoKey Writer v0.1.0

首个可分发版本。

## 包含内容

- macOS 原生桌面应用
- 支持模拟逐字输入到当前激活窗口
- 支持输入速度、倒计时、换行停顿设置
- 支持 `Esc` 紧急停止
- 支持权限状态检查与系统设置跳转
- 提供 `.app` 与 `.zip` 打包产物

## 已知限制

- 仅支持 macOS
- 某些编辑器会对引号、括号等符号做自动补全，可能影响最终输入效果
- 依赖系统辅助功能与输入监控权限
